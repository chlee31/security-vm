"""Group the Python modules that make up the Security VM application.

The package contains the sensor readers, normalizers, SQLite data layer, AI
client, worker orchestration, and FastAPI dashboard. This file has no runtime
logic; it simply allows those modules to be imported with names such as
``app.database`` and ``app.main``.
"""
